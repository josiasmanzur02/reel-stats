{
  "name": "fish-catch-tracker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "python3 -m http.server 5173",
    "build": "rm -rf dist && mkdir -p dist && cp -R assets dist/ && cp *.html manifest.webmanifest service-worker.js dist/",
    "preview": "python3 -m http.server 4173 --directory dist"
  }
}
